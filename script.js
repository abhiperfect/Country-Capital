function myFunction( val ){
    if( val === "india")
        document.querySelector("#capital").textContent = "Delhi"; 
    if( val === "china")
        document.querySelector("#capital").textContent = "Beijing"; 
    if( val === "japan")
        document.querySelector("#capital").textContent = "Tokyo"; 
    if( val === "russia")
        document.querySelector("#capital").textContent = "Moscow"; 
    if( val === "ukrain")
        document.querySelector("#capital").textContent = "Kyiv"; 
    
}